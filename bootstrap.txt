'use strict';
// ============================================================
// foobar2000 x Spotify  --  JSplitter dev-loop bootstrap
// ------------------------------------------------------------
// OPTIONAL. Use this ONLY after the paste-inline PoC works and
// you want the fast edit->reload loop.
//
// Instead of pasting main.js into the panel, paste THIS one line.
// It loads our app from the profile scripts folder that
// deploy.ps1 writes to. Then iterate by editing src/main.js +
// running deploy.ps1, and reload the panel (right-click > Reload).
//
// main.js already calls window.DefineScript, so we don't here.
// ============================================================

include(fb.ProfilePath + 'scripts\\foobar-spotify\\main.js');

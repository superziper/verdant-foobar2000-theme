'use strict';
// ============================================================
// foobar2000 x Spotify  --  panel bootstrap
// ------------------------------------------------------------
// Paste THIS (the whole block) into the JavaScript Panel's
// Configuration editor, then press Ctrl+S.
//
// It does three things:
//   1. names the script,
//   2. loads the component's bundled helpers (RGB, Scale, fonts,
//      DWRITE_* constants),
//   3. loads OUR app from the foobar profile scripts folder.
//
// You paste this ONCE. After that, iterate by editing
// src/main.js and running deploy.ps1 -- this bootstrap keeps
// pulling the latest main.js from disk on every panel reload.
// ============================================================

window.DefineScript('Spotify for foobar2000', { author: 'zulvanavivi' });
include(fb.ComponentPath + 'helpers.js');
include(fb.ProfilePath + 'scripts\\foobar-spotify\\main.js');

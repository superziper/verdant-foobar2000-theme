'use strict';
// ============================================================
// Spotify for foobar2000 -- dev-loop bootstrap (OPTIONAL)
// ------------------------------------------------------------
// For a normal install, paste src/main.js into the panel and
// ignore this file.
//
// For the fast edit -> reload loop, run deploy.ps1 (which copies
// src\ into the profile scripts folder) and paste THIS one line
// into the panel instead. Then edit src/main.js and reload the
// panel (right-click > Reload); no foobar restart needed.
//
// main.js already calls window.DefineScript, so we don't here.
// ============================================================

include(fb.ProfilePath + 'scripts\\foobar-spotify\\main.js');
